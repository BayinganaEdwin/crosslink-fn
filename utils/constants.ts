const PERSIST_KEY = 'CrossLink';
const DATE_FORMAT = 'DD MMM YYYY';
const TOKEN_NAME = 'crossLink_token';
const USER_DATA = 'crosslink_user';

const TIME_FORMAT = 'HH:mm A';

export { DATE_FORMAT, TIME_FORMAT, PERSIST_KEY, TOKEN_NAME, USER_DATA };
